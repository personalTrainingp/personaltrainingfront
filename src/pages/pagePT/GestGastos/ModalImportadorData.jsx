import { Dialog } from 'primereact/dialog'
import React from 'react'

export const ModalImportadorData = ({onShow, onHide}) => {
  return (
    <Dialog onShow={onShow} onHide={onHide}>
        ModalImportadorData
    </Dialog>
  )
}
